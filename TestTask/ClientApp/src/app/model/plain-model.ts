import { PlainEnum } from './plain-enum';

export class PlainModel {
    id: number
    name: string
    enum: number;
    date: Date
    firstSwitcher: boolean;
    secondSwitcher: boolean;
    range: number;
}
